---
# vars file for jenkins

jenkins_supported:
  - xenial
  - bionic
jenkins_init_system: 'systemd'
jenkins_log_dir: '/var/log/jenkins'
jenkins_cache_dir: '/var/cache/jenkins'
jenkins_conf_file: '/etc/default/jenkins'

# Jenkins requirement
java_version: '8'
